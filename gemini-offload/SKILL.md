---
name: gemini-offload
description: Deprecated — use extract-content instead. Redirects to extract-content skill.
---

# Deprecated — Use `extract-content`

This skill has been renamed to **`extract-content`**, which adds local Python extraction for text-embedded PDFs (no LLM needed) with Gemini as fallback for scanned PDFs and images.

Invoke the `extract-content` skill instead.
